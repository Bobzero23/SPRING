/*@Component using this annotation we can make our life a little easier, by just mentioning the  annotation at the
* top of the class the Spring framework will figure out itself how to create objects, and we must know while we are
* using this annotation the programmers have no control of the program it is Spring framework, and it is programmer's
* responsibility to mention in the bean class the packages where the components to be located */