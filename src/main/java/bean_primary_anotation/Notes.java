/*@Primary this annotation is used when we have multiple beans with same name and when we call them without parameter
* so the compiler will give us one primary bean with primary annotation as default and don't put multiple primaries */